import { MarkdownModalLinkDirective } from './markdown-modal-link.directive';

describe('MarkdownLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkdownModalLinkDirective();
    expect(directive).toBeTruthy();
  });
});
